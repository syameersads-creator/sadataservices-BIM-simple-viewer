import { ApsServiceRequestConfig } from './apsServiceRequestConfig';
import { IResiliencyConfiguration } from '../index';
export declare class ApsService {
    private readonly retryPolicy;
    private readonly circuitBreakerPolicy;
    private readonly timeoutPolicy;
    private readonly resiliencePolicy;
    private static readonly RETRYABLE_STATUS_CODES;
    constructor(config: IResiliencyConfiguration);
    private createRetryPolicy;
    private createCircuitBreakerPolicy;
    private createTimeoutPolicy;
    request(config: ApsServiceRequestConfig): Promise<any>;
}
