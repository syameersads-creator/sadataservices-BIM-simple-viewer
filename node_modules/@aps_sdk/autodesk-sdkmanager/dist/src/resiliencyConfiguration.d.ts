import { IResiliencyConfiguration } from './iResiliencyConfiguration';
export declare class ResiliencyConfiguration implements IResiliencyConfiguration {
    private _retryCount;
    private _backoffInterval;
    private _circuitBreakerInterval;
    private _timeoutValue;
    constructor(retryCount?: number, backoffInterval?: number, circuitBreakerInterval?: number, timeoutValue?: number);
    get retryCount(): number;
    set retryCount(retryCount: number);
    get backoffInterval(): number;
    get timeOutValue(): number;
    set timeOutValue(timeOutValue: number);
    set backoffInterval(backoffInterval: number);
    set circuitBreakerInterval(circuitBreakerInterval: number);
    get circuitBreakerInterval(): number;
    toString: () => string;
}
